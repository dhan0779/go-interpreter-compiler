To create module: `go mod init interpreter`. Then, we can start adding packages (subfolders) and import using `interpreter\package`
To run the tests: Go to the specific package folder and run `go test ./package`
To run main.go: `go run main.go`